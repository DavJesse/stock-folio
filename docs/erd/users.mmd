erDiagram
    users {
        %% Primary key uniquely identifies each user
        INTEGER id PK
        %% Stores user's email address for authentication and contact
        TEXT email
        %% Securely stores hashed password, never plain text
        TEXT password_hash
        %% Timestamp for when the user account was created
        DATETIME created_at
    }
